import React from 'react';
import { Link } from "react-router-dom";

const Catalogo = (props) => {
    return (
        <div>
            <div>Catalogo</div>
        </div>
    );
}

export default Catalogo;