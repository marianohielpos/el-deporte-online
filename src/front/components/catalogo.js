import React from 'react';
import { Link } from "react-router-dom";

const Catalogo = (props) => {
    return (
        <div>
            <div>Catalogo</div>
            <Link to={'/'}>Home</Link>
        </div>
    );
}

export default Catalogo;